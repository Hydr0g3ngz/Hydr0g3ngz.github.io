// Compatibility shim for existing website checkouts. The adapter is website code;
// the standalone Studio application does not depend on this module.
export { default } from '../src/studio-adapter/integration.mjs';
